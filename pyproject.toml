[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rtools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "torch",
  "scikit-learn",
]
authors = [{ name = "Andrea Pollastro" }]

